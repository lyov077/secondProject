/**const { expect } = require("chai");
const { ethers } = require("hardhat");

describe("Greeter", function () {
  it("Should return the new greeting once it's changed", async function () {
    /*const Greeter = await ethers.getContractFactory("Greeter");
    const greeter = await Greeter.deploy("Hello, world!");
    await greeter.deployed();
    const {deployer} = await getNamedAccounts()
    await deployments.fixture();
    
    const greeter = await ethers.getContract("Greeter", deployer);
    console.log("line 13 ok", greeter);
    
    let msg = await greeter.greet();
    console.log("line 16", msg);
    
    const tx = await greeter.setGreeting("hello class");
    console.log("line 19", tx);

    msg = await greeter.greet();
    console.log("line 22", msg)

    //expect(await greeter.greet()).to.equal("Hello, world!");

    //const setGreetingTx = await greeter.setGreeting("Hola, mundo!");

    // wait until the transaction is mined
    //await setGreetingTx.wait();

    //expect(await greeter.greet()).to.equal("Hola, mundo!");
  });
});

**/